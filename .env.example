# Copy to `.env` for local overrides. Never commit real secrets.
#
# Base public path for the build. Leave as "/" for Vercel/Netlify/root hosts.
# For a GitHub Pages PROJECT site, set to "/<your-repo-name>/".
# (The CI workflow sets this automatically — you usually don't need it locally.)
VITE_BASE=/
